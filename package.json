{
  "name": "echo-front",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.mjs && node scripts/architecture-check.mjs",
    "check": "npm test && wrangler deploy --dry-run"
  },
  "dependencies": {
    "@dimforge/rapier3d": "0.19.3"
  },
  "devDependencies": {
    "wrangler": "^4.120.1"
  }
}
