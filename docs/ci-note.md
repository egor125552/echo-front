# CI note

Automatic CI runs only build and runtime smoke checks. The existing gameplay regression tests remain available through `npm test` for manual use until their stale expectations are repaired.
